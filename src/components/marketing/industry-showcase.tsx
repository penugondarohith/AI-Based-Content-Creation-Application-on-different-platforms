import { ArrowUpRight } from "lucide-react";
import { industries } from "@/config/industries";

export function IndustryShowcase() {
  return <section id="industries" className="mx-auto max-w-7xl px-5 py-24 lg:px-8"><div className="mb-12 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="mb-3 text-xs font-semibold uppercase tracking-[.2em] text-primary">Built with context</p><h2 className="max-w-xl text-4xl font-semibold tracking-[-.04em] text-foreground">Different industries. Different intelligence.</h2></div><p className="max-w-sm text-sm leading-6 text-muted">Your content deserves more than a generic prompt. Start with the details that make your category matter.</p></div><div className="grid gap-4 md:grid-cols-2">{industries.map((industry, index) => <article key={industry.id} className="group relative overflow-hidden rounded-xl border border-border bg-surface p-6 transition duration-300 hover:-translate-y-1 hover:border-primary/30 hover:shadow-xl hover:shadow-primary/5"><div className="mb-12 flex items-start justify-between"><span className="flex size-11 items-center justify-center rounded-xl text-2xl" style={{ backgroundColor: `${industry.color}18`, color: industry.color }}>{industry.icon}</span><span className="text-xs text-muted">0{index + 1}</span></div><h3 className="text-xl font-semibold text-foreground">{industry.name}</h3><p className="mt-2 max-w-sm text-sm leading-6 text-muted">{industry.description}</p><div className="mt-6 flex flex-wrap gap-2">{industry.focusAreas.slice(0, 4).map((focus) => <span key={focus} className="rounded-md bg-surface-muted px-2.5 py-1.5 text-xs text-muted">{focus}</span>)}</div><ArrowUpRight className="absolute bottom-6 right-6 text-border transition group-hover:text-primary" size={20} /></article>)}</div></section>;
}