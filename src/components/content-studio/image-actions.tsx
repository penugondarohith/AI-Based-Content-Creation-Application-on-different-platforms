import { Download, Expand, RefreshCw } from "lucide-react";
import type { GeneratedImage } from "@/types/generated-image";
export function ImageActions({ image, loading, onRegenerate }: { image: GeneratedImage; loading: boolean; onRegenerate: () => void }) { const download = () => { if (!image.imageUrl) return; const link = document.createElement("a"); link.href = image.imageUrl; link.download = `contentforge-${image.generatedContentId}.svg`; link.click(); }; return <div className="flex flex-wrap gap-2"><button type="button" onClick={onRegenerate} disabled={loading} className="flex items-center gap-1.5 rounded-lg border border-border px-3 py-2 text-xs text-muted hover:bg-surface-muted disabled:opacity-50"><RefreshCw size={13} className={loading ? "animate-spin" : ""} /> Regenerate visual</button><button type="button" onClick={download} className="flex items-center gap-1.5 rounded-lg bg-primary px-3 py-2 text-xs font-medium text-white hover:bg-primary-strong"><Download size={13} /> Download visual</button><a href={image.imageUrl} target="_blank" rel="noreferrer" className="flex items-center gap-1.5 rounded-lg border border-border px-3 py-2 text-xs text-muted hover:bg-surface-muted"><Expand size={13} /> Expand</a></div>; }