import type { CalendarBlueprintPost } from "@/types/content-strategy";
import type { GeneratedContent } from "@/types/generated-content";
export function CampaignPostSidebar({ posts, generated, selectedId, onSelect }: { posts: CalendarBlueprintPost[]; generated: GeneratedContent[]; selectedId: string; onSelect: (id: string) => void }) { return <aside className="rounded-xl border border-border bg-surface p-4 lg:h-fit lg:sticky lg:top-6"><div className="flex items-center justify-between"><div><p className="text-[10px] font-semibold uppercase tracking-[.18em] text-primary">Campaign posts</p><h2 className="mt-1 text-lg font-semibold">{posts.length} blueprints</h2></div><span className="rounded-full bg-primary-soft px-2.5 py-1 text-[10px] text-primary">{generated.length} ready</span></div><div className="mt-5 max-h-[620px] space-y-1.5 overflow-y-auto">{posts.map((post) => { const item = generated.find((content) => content.calendarPostId === post.id); const selected = selectedId === post.id; return <button type="button" key={post.id} onClick={() => onSelect(post.id)} className={`w-full rounded-lg border p-3 text-left transition focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary ${selected ? "border-primary bg-primary-soft" : "border-transparent hover:border-border hover:bg-surface-muted"}`}><div className="flex items-center justify-between"><span className="text-[10px] font-semibold uppercase tracking-wider text-muted">Post {String(post.postNumber).padStart(2, "0")}</span><span className={`text-[9px] font-medium ${item?.status === "READY" || item?.status === "EDITED" ? "text-success" : item?.status === "ERROR" ? "text-danger" : "text-muted"}`}>{item?.status ?? "NOT GENERATED"}</span></div><p className="mt-2 line-clamp-1 text-xs font-medium">{post.contentTheme}</p><p className="mt-1 line-clamp-1 text-[10px] text-muted">{post.objective}</p></button>; })}</div></aside>; }