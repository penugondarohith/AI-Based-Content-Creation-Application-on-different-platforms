import type { ImageAspectRatio } from "@/types/generated-image";
const ratios: Array<[ImageAspectRatio, string]> = [["1:1", "Square"], ["4:5", "Portrait"], ["16:9", "Landscape"], ["9:16", "Story"]];
export function ImageRatioSelector({ value, onChange }: { value: ImageAspectRatio; onChange: (value: ImageAspectRatio) => void }) { return <div className="flex flex-wrap gap-2">{ratios.map(([ratio, label]) => <button key={ratio} type="button" onClick={() => onChange(ratio)} className={`rounded-md border px-2.5 py-1.5 text-xs transition focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary ${value === ratio ? "border-primary bg-primary text-white" : "border-border text-muted hover:border-primary/30"}`}>{label} <span className="ml-1 opacity-60">{ratio}</span></button>)}</div>; }