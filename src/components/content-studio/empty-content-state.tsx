import { ArrowRight, Sparkles } from "lucide-react";
export function EmptyContentState({ onGenerateFirst, onGenerateAll }: { onGenerateFirst: () => void; onGenerateAll: () => void }) { return <div className="rounded-xl border border-border bg-surface p-10 text-center"><div className="mx-auto flex size-14 items-center justify-center rounded-2xl bg-primary-soft text-primary"><Sparkles size={24} /></div><h2 className="mt-6 text-2xl font-semibold tracking-[-.03em]">Your campaign content is ready to be created</h2><p className="mx-auto mt-3 max-w-lg text-sm leading-6 text-muted">Your strategy and calendar are complete. Generate content for each campaign post or generate the complete campaign at once.</p><div className="mt-7 flex flex-wrap justify-center gap-3"><button type="button" onClick={onGenerateFirst} className="flex items-center gap-2 rounded-lg border border-border px-4 py-2.5 text-sm font-medium hover:bg-surface-muted">Generate first post <ArrowRight size={15} /></button><button type="button" onClick={onGenerateAll} className="rounded-lg bg-primary px-4 py-2.5 text-sm font-medium text-white hover:bg-primary-strong">Generate all content</button></div></div>; }