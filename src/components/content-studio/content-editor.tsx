import { Save } from "lucide-react";
import type { GeneratedContent, GeneratedPostContent } from "@/types/generated-content";
import { HashtagEditor } from "./hashtag-editor";
import { VisualDirectionPanel } from "./visual-direction-panel";
export function ContentEditor({ item, onChange, onSave, onRegenerate }: { item: GeneratedContent; onChange: (content: GeneratedPostContent) => void; onSave: () => void; onRegenerate: () => void }) { const update = (patch: Partial<GeneratedPostContent>) => onChange({ ...item.content, ...patch }); return <section className="rounded-xl border border-border bg-surface p-5 sm:p-6"><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] font-semibold uppercase tracking-[.18em] text-primary">Content editor</p><h2 className="mt-2 text-xl font-semibold">Post {String(item.postNumber).padStart(2, "0")}</h2></div><div className="flex gap-2"><button type="button" onClick={onRegenerate} className="rounded-lg border border-border px-3 py-2 text-xs text-muted hover:bg-surface-muted">Regenerate</button><button type="button" onClick={onSave} className="flex items-center gap-1.5 rounded-lg bg-primary px-3 py-2 text-xs font-medium text-white hover:bg-primary-strong"><Save size={13} /> Save edits</button></div></div><div className="mt-7 space-y-5"><EditorField label="Post title" value={item.content.title} onChange={(value) => update({ title: value })} /><EditorField label="Hook" value={item.content.hook} onChange={(value) => update({ hook: value })} /><EditorField label="Caption" value={item.content.caption} multiline onChange={(value) => update({ caption: value })} /><EditorField label="Call to action" value={item.content.callToAction} onChange={(value) => update({ callToAction: value })} /><div><p className="text-xs font-medium">Hashtags</p><div className="mt-2"><HashtagEditor hashtags={item.content.hashtags} onChange={(hashtags) => update({ hashtags })} /></div></div><div><p className="text-xs font-medium">Visual direction</p><div className="mt-2"><VisualDirectionPanel direction={item.content.visualDirection} onChange={(visualDirection) => update({ visualDirection })} /></div></div></div></section>; }
function EditorField({ label, value, onChange, multiline }: { label: string; value: string; onChange: (value: string) => void; multiline?: boolean }) { const className = "mt-1.5 w-full rounded-lg border border-border bg-surface-muted p-3 text-sm leading-6 outline-none transition focus:border-primary focus:ring-2 focus:ring-primary/15"; return <label className="block text-xs font-medium">{label}{multiline ? <textarea value={value} onChange={(event) => onChange(event.target.value)} className={`${className} min-h-32 resize-y`} /> : <input value={value} onChange={(event) => onChange(event.target.value)} className={className} />}</label>; }