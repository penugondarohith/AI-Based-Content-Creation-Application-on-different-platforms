const styles = ["Commercial Photography", "Editorial", "Minimal", "Luxury", "Lifestyle", "Cinematic", "Product Focus"];
export function VisualStyleSelector({ value, onChange }: { value: string; onChange: (value: string) => void }) { return <div className="flex flex-wrap gap-2">{styles.map((style) => <button key={style} type="button" onClick={() => onChange(style)} className={`rounded-md border px-2.5 py-1.5 text-xs transition focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary ${value === style ? "border-primary bg-primary-soft text-primary-strong" : "border-border text-muted hover:border-primary/30"}`}>{style}</button>)}</div>; }