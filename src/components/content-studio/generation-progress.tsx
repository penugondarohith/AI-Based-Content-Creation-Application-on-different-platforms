export function GenerationProgress({ completed, total, current }: { completed: number; total: number; current: string }) { const percent = Math.round((completed / total) * 100); return <div role="status" aria-live="polite" className="rounded-xl border border-primary/20 bg-primary-soft p-4"><div className="flex items-center justify-between text-xs font-medium text-primary-strong"><span>Generating campaign content</span><span>{completed} of {total} complete</span></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-white"><div className="h-full rounded-full bg-primary transition-all" style={{ width: `${percent}%` }} /></div><p className="mt-2 text-xs text-primary">Creating {current}...</p></div>; }