import type { VisualDirection } from "@/types/generated-content";
export function VisualDirectionPanel({ direction, onChange }: { direction: VisualDirection; onChange: (direction: VisualDirection) => void }) { const fields: Array<[keyof VisualDirection, string]> = [["concept", "Concept"], ["composition", "Composition"], ["subjectFocus", "Subject focus"], ["backgroundSuggestion", "Background"], ["typographySuggestion", "Typography"]]; return <div className="grid gap-3 sm:grid-cols-2">{fields.map(([key, label]) => <label key={key} className="text-xs font-medium">{label}<textarea value={String(direction[key] ?? "")} onChange={(event) => onChange({ ...direction, [key]: event.target.value })} className="mt-1.5 min-h-16 w-full resize-y rounded-lg border border-border bg-surface-muted p-2.5 text-xs leading-5 outline-none focus:border-primary focus:ring-2 focus:ring-primary/15" /></label>)}<div className="sm:col-span-2"><p className="text-xs font-medium">Mood & color direction</p><p className="mt-1.5 text-xs text-muted">{direction.mood.join(" · ")} {direction.colorDirection?.length ? ` · ${direction.colorDirection.join(" · ")}` : ""}</p></div></div>; }