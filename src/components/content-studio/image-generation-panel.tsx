import { useState } from "react";
import { ImageIcon, LoaderCircle } from "lucide-react";
import type { GeneratedImage, ImageAspectRatio } from "@/types/generated-image";
import { ImagePreviewCard } from "./image-preview-card";
import { ImageRatioSelector } from "./image-ratio-selector";
import { VisualStyleSelector } from "./visual-style-selector";
export function ImageGenerationPanel({ image, loading, onGenerate }: { image?: GeneratedImage; loading: boolean; onGenerate: (ratio: ImageAspectRatio, style: string) => void }) { const [ratio, setRatio] = useState<ImageAspectRatio>(image?.prompt.aspectRatio ?? "4:5"); const [style, setStyle] = useState(image?.prompt.visualStyle ?? "Editorial"); return <section className="rounded-xl border border-border bg-surface p-5 sm:p-6"><div className="flex items-start justify-between gap-3"><div><p className="flex items-center gap-2 text-[10px] font-semibold uppercase tracking-[.18em] text-primary"><ImageIcon size={14} /> AI Visual</p><h2 className="mt-2 text-xl font-semibold">Generate a strategy-aligned visual</h2><p className="mt-2 text-xs leading-5 text-muted">Your visual will use the industry, brand, calendar theme, and generated post context.</p></div>{image && <span className="rounded-full bg-emerald-50 px-2.5 py-1 text-[10px] font-medium text-success">{image.status}</span>}</div><div className="mt-5 space-y-4"><div><p className="mb-2 text-xs font-medium">Aspect ratio</p><ImageRatioSelector value={ratio} onChange={setRatio} /></div><div><p className="mb-2 text-xs font-medium">Visual style</p><VisualStyleSelector value={style} onChange={setStyle} /></div>{image ? <ImagePreviewCard image={image} loading={loading} onRegenerate={() => onGenerate(ratio, style)} /> : <button type="button" onClick={() => onGenerate(ratio, style)} disabled={loading} className="flex w-full items-center justify-center gap-2 rounded-lg bg-primary px-4 py-3 text-sm font-medium text-white hover:bg-primary-strong disabled:opacity-60">{loading ? <><LoaderCircle size={16} className="animate-spin" /> Generating strategy-aligned visual...</> : <>Generate visual <ImageIcon size={16} /></>}</button>}</div></section>; }