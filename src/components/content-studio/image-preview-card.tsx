/* eslint-disable @next/next/no-img-element */
import type { GeneratedImage } from "@/types/generated-image";
import { ImageActions } from "./image-actions";
export function ImagePreviewCard({ image, loading, onRegenerate }: { image: GeneratedImage; loading: boolean; onRegenerate: () => void }) { return <div className="overflow-hidden rounded-xl border border-border bg-surface"><div className="relative overflow-hidden bg-surface-muted"><img src={image.imageUrl} alt={`Generated visual for ${image.prompt.subject}`} className="max-h-[520px] w-full object-contain" /></div><div className="space-y-3 p-4"><div className="flex items-center justify-between text-xs"><span className="font-medium">Generated visual</span><span className="rounded-full bg-emerald-50 px-2 py-1 text-success">{image.status}</span></div><p className="text-[11px] text-muted">{image.prompt.visualStyle} · {image.prompt.aspectRatio} · {image.metadata?.provider}</p><ImageActions image={image} loading={loading} onRegenerate={onRegenerate} /></div></div>; }