import { Plus, X } from "lucide-react";
import { useState } from "react";
export function HashtagEditor({ hashtags, onChange }: { hashtags: string[]; onChange: (hashtags: string[]) => void }) { const [value, setValue] = useState(""); const add = () => { const tag = `#${value.replace(/^#/, "").replace(/\s/g, "")}`; if (tag.length > 1 && !hashtags.includes(tag)) onChange([...hashtags, tag]); setValue(""); }; return <div><div className="flex flex-wrap gap-2">{hashtags.map((tag) => <span key={tag} className="inline-flex items-center gap-1 rounded-md bg-primary-soft px-2 py-1 text-xs text-primary-strong">{tag}<button type="button" onClick={() => onChange(hashtags.filter((item) => item !== tag))} aria-label={`Remove ${tag}`}><X size={12} /></button></span>)}</div><div className="mt-3 flex gap-2"><input value={value} onChange={(event) => setValue(event.target.value)} onKeyDown={(event) => event.key === "Enter" && (event.preventDefault(), add())} placeholder="Add hashtag" aria-label="Add hashtag" className="min-w-0 flex-1 rounded-lg border border-border bg-surface-muted px-3 py-2 text-xs outline-none focus:border-primary" /><button type="button" onClick={add} aria-label="Add hashtag" className="rounded-lg border border-border p-2 text-muted hover:bg-surface-muted hover:text-primary"><Plus size={15} /></button></div></div>; }