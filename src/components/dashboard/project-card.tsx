import Link from "next/link";
import { ArrowUpRight } from "lucide-react";
import type { Project } from "@/types/project";
import { getIndustry } from "@/config/industries";

const statusStyles = { READY: "bg-emerald-50 text-success", DRAFT: "bg-amber-50 text-warning", CONFIGURING: "bg-primary-soft text-primary", GENERATING: "bg-violet-50 text-violet-700", COMPLETED: "bg-sky-50 text-sky-700" };
export function ProjectCard({ project }: { project: Project }) { const industry = getIndustry(project.industry); return <article className="group rounded-xl border border-border bg-surface p-5 transition hover:border-primary/30 hover:shadow-lg hover:shadow-primary/5"><div className="flex items-start justify-between"><div className="flex items-center gap-3"><span className="flex size-9 items-center justify-center rounded-lg text-xl" style={{ backgroundColor: `${industry.color}18`, color: industry.color }}>{industry.icon}</span><div><h3 className="text-sm font-semibold">{project.name}</h3><p className="mt-1 text-[11px] uppercase tracking-wider text-muted">{industry.shortName}</p></div></div><span className={`rounded-full px-2.5 py-1 text-[10px] font-medium ${statusStyles[project.status]}`}>{project.status}</span></div><div className="mt-6 flex gap-6 text-xs text-muted"><span>{project.duration}</span><span>{project.postCount} posts</span></div><div className="mt-5 flex items-center justify-between border-t border-border pt-4"><span className="text-xs text-muted">Updated {project.updatedAt}</span><Link href="#" className="flex items-center gap-1 text-xs font-medium text-primary opacity-0 transition group-hover:opacity-100">View project <ArrowUpRight size={13} /></Link></div></article>; }