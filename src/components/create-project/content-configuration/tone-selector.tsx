import { tonePreferences } from "@/config/tones";
import type { TonePreference } from "@/types/content-configuration";
export function ToneSelector({ industryTone, value, onChange }: { industryTone: string[]; value: TonePreference[]; onChange: (value: TonePreference[]) => void }) { const toggle = (id: TonePreference) => value.includes(id) ? onChange(value.filter((tone) => tone !== id)) : value.length < 3 ? onChange([...value, id]) : undefined; return <section><h3 className="text-sm font-semibold">Preferred Communication Tone</h3><p className="mt-1 text-xs text-muted">Keep Industry DNA intact, then add up to three preferences.</p><div className="mt-4"><p className="text-[10px] font-semibold uppercase tracking-wider text-muted">Industry DNA tone</p><div className="mt-2 flex flex-wrap gap-2">{industryTone.map((tone) => <span key={tone} className="rounded-md bg-primary-soft px-2.5 py-1.5 text-xs text-primary-strong">{tone}</span>)}</div></div><div className="mt-4"><div className="flex items-center justify-between"><p className="text-[10px] font-semibold uppercase tracking-wider text-muted">Additional preferences</p><span className="text-[10px] text-muted">{value.length}/3 selected</span></div><div className="mt-2 flex flex-wrap gap-2">{tonePreferences.map(({ id, name }) => <button key={id} type="button" onClick={() => toggle(id)} className={`rounded-md border px-2.5 py-1.5 text-xs transition focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary ${value.includes(id) ? "border-primary bg-primary text-white" : "border-border bg-surface text-muted hover:border-primary/30"}`}>{name}</button>)}</div></div></section>; }