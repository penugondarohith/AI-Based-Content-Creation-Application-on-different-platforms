import { contentFormats } from "@/config/content-formats";
import type { ContentFormat } from "@/types/content-configuration";
import { Check } from "lucide-react";
export function ContentFormatSelector({ value, onChange }: { value: ContentFormat[]; onChange: (value: ContentFormat[]) => void }) { const toggle = (id: ContentFormat) => onChange(value.includes(id) ? value.filter((item) => item !== id) : [...value, id]); return <section><h3 className="text-sm font-semibold">Content Formats</h3><p className="mt-1 text-xs text-muted">Select every format that belongs in the mix.</p><div className="mt-3 grid gap-2 sm:grid-cols-2">{contentFormats.map(({ id, name, description, icon: Icon }) => { const selected = value.includes(id); return <button key={id} type="button" onClick={() => toggle(id)} className={`flex items-center gap-3 rounded-lg border p-3 text-left transition focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary ${selected ? "border-primary bg-primary-soft" : "border-border bg-surface hover:border-primary/30"}`}><span className={`flex size-7 shrink-0 items-center justify-center rounded-md ${selected ? "bg-primary text-white" : "bg-surface-muted text-muted"}`}>{selected ? <Check size={14} /> : <Icon size={14} />}</span><span><span className="block text-xs font-medium">{name}</span><span className="mt-1 block text-[11px] text-muted">{description}</span></span></button>; })}</div></section>; }