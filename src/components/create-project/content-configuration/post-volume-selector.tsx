import type { ContentConfiguration } from "@/types/content-configuration";
import { recommendedPostCounts } from "@/lib/content-configuration";
export function PostVolumeSelector({ duration, value, custom, onChange }: { duration: ContentConfiguration["duration"]; value: 3 | 6 | 12; custom: boolean; onChange: (value: 3 | 6 | 12) => void }) { return <section><div className="flex items-center justify-between"><div><h3 className="text-sm font-semibold">Content Volume</h3><p className="mt-1 text-xs text-muted">Choose the number of posts to plan.</p></div>{custom ? <span className="rounded-full bg-amber-50 px-2.5 py-1 text-[10px] font-medium text-warning">Custom plan</span> : <span className="text-[10px] text-primary">Recommended</span>}</div><div className="mt-3 flex gap-2">{([3, 6, 12] as const).map((count) => <button key={count} type="button" onClick={() => onChange(count)} className={`flex-1 rounded-lg border py-3 text-sm transition focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary ${value === count ? "border-primary bg-primary-soft font-medium text-primary-strong" : "border-border bg-surface hover:border-primary/30"}`}>{count} Posts{recommendedPostCounts[duration] === count && <span className="mt-1 block text-[10px] font-normal text-primary">Suggested</span>}</button>)}</div></section>; }