import { audiences } from "@/config/audiences";
import type { AudienceType } from "@/types/content-configuration";
export function AudienceSelector({ value, customAudience, onChange, onCustomAudienceChange }: { value: AudienceType | null; customAudience: string; onChange: (value: AudienceType) => void; onCustomAudienceChange: (value: string) => void }) { return <section><h3 className="text-sm font-semibold">Target Audience</h3><p className="mt-1 text-xs text-muted">Who should this strategy speak to?</p><div className="mt-3 flex flex-wrap gap-2">{audiences.map(({ id, name }) => <button key={id} type="button" onClick={() => onChange(id)} className={`rounded-lg border px-3 py-2 text-xs transition focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary ${value === id ? "border-primary bg-primary-soft font-medium text-primary-strong" : "border-border bg-surface text-muted hover:border-primary/30"}`}>{name}</button>)}</div>{value === "CUSTOM_AUDIENCE" && <textarea value={customAudience} onChange={(event) => onCustomAudienceChange(event.target.value)} placeholder="Describe your target audience" aria-label="Describe your target audience" className="mt-3 min-h-24 w-full resize-y rounded-lg border border-border bg-surface p-3 text-sm outline-none transition placeholder:text-muted/70 focus:border-primary focus:ring-2 focus:ring-primary/15" />}</section>; }