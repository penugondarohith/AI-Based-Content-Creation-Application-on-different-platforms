import { contentGoals } from "@/config/content-goals";
import type { ContentGoal } from "@/types/content-configuration";
export function ContentGoalSelector({ value, onChange }: { value: ContentGoal | null; onChange: (value: ContentGoal) => void }) { return <section><h3 className="text-sm font-semibold">What is your primary goal?</h3><p className="mt-1 text-xs text-muted">Choose the outcome this strategy should prioritize.</p><div className="mt-3 grid gap-2 sm:grid-cols-2">{contentGoals.map(({ id, name, description, icon: Icon }) => <button key={id} type="button" onClick={() => onChange(id)} className={`flex items-start gap-3 rounded-lg border p-3 text-left transition focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary ${value === id ? "border-primary bg-primary-soft" : "border-border bg-surface hover:border-primary/30"}`}><span className={`mt-0.5 flex size-7 shrink-0 items-center justify-center rounded-md ${value === id ? "bg-primary text-white" : "bg-surface-muted text-muted"}`}><Icon size={14} /></span><span><span className="block text-xs font-medium">{name}</span><span className="mt-1 block text-[11px] leading-4 text-muted">{description}</span></span></button>)}</div></section>; }