"use client";
import { Check } from "lucide-react";
import { motion } from "framer-motion";
import type { IndustryConfig } from "@/types/industry";
import { cn } from "@/lib/utils";

export function IndustryCard({ industry, selected, onSelect }: { industry: IndustryConfig; selected: boolean; onSelect: () => void }) { return <motion.button type="button" whileHover={{ y: -2 }} whileTap={{ scale: .99 }} onClick={onSelect} className={cn("relative text-left rounded-xl border bg-surface p-5 transition", selected ? "border-primary bg-primary-soft/40 shadow-lg shadow-primary/10" : "border-border hover:border-primary/30 hover:shadow-md")}><div className="flex items-start justify-between"><span className="flex size-10 items-center justify-center rounded-lg text-xl" style={{ backgroundColor: `${industry.color}18`, color: industry.color }}>{industry.icon}</span>{selected && <span className="flex size-6 items-center justify-center rounded-full bg-primary text-white"><Check size={14} /></span>}</div><h3 className="mt-6 font-semibold">{industry.name}</h3><p className="mt-2 min-h-12 text-xs leading-5 text-muted">{industry.description}</p><div className="mt-4 flex flex-wrap gap-1.5">{industry.tone.map((tone) => <span key={tone} className="rounded bg-surface-muted px-2 py-1 text-[10px] text-muted">{tone}</span>)}</div><div className="mt-5 border-t border-border pt-4"><p className="text-[10px] font-semibold uppercase tracking-wider text-muted">Focus areas</p><p className="mt-2 line-clamp-2 text-xs leading-5 text-foreground/70">{industry.focusAreas.join(" · ")}</p></div></motion.button>; }