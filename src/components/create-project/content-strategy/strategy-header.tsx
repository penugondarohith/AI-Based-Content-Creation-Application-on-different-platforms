import { Pencil, RefreshCw } from "lucide-react";
export function StrategyHeader({ name, summary, loading, onRegenerate, onEdit }: { name: string; summary: string; loading: boolean; onRegenerate: () => void; onEdit: () => void }) { return <header className="rounded-xl bg-[#222149] p-6 text-white sm:p-8"><div className="flex flex-col justify-between gap-6 md:flex-row md:items-start"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-violet-200">Content strategy</p><h1 className="mt-3 max-w-2xl text-3xl font-semibold tracking-[-.04em] sm:text-4xl">{name}</h1><p className="mt-4 max-w-2xl text-sm leading-6 text-white/60">{summary}</p></div><div className="flex shrink-0 gap-2"><button type="button" onClick={onEdit} className="inline-flex items-center gap-2 rounded-lg border border-white/15 px-3 py-2 text-xs text-white/75 transition hover:border-white/35 hover:text-white"><Pencil size={14} /> Edit</button><button type="button" disabled={loading} onClick={onRegenerate} className="inline-flex items-center gap-2 rounded-lg bg-white px-3 py-2 text-xs font-medium text-primary-strong transition hover:bg-violet-100 disabled:opacity-60"><RefreshCw size={14} className={loading ? "animate-spin" : ""} /> {loading ? "Rebuilding..." : "Regenerate"}</button></div></div></header>; }