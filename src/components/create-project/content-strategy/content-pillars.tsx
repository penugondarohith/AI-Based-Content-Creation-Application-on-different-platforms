import { motion } from "framer-motion";
import type { ContentPillar } from "@/types/content-strategy";
import { SectionHeading } from "./strategy-overview";
export function ContentPillars({ pillars }: { pillars: ContentPillar[] }) { return <section><SectionHeading eyebrow="Content architecture" title="Content pillars" /><div className="mt-4 grid gap-3 md:grid-cols-2">{pillars.map((pillar, index) => <motion.article key={pillar.id} initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: index * .04 }} className="rounded-xl border border-border bg-surface p-5"><div className="flex items-start justify-between gap-4"><div><span className="text-[10px] font-semibold uppercase tracking-wider text-primary">0{index + 1}</span><h3 className="mt-2 text-sm font-semibold">{pillar.title}</h3></div><span className="text-lg font-semibold tracking-[-.03em] text-primary">{pillar.percentage}%</span></div><div className="mt-4 h-1.5 overflow-hidden rounded-full bg-primary-soft"><motion.div initial={{ width: 0 }} animate={{ width: `${pillar.percentage}%` }} transition={{ duration: .5 }} className="h-full rounded-full bg-primary" /></div><p className="mt-4 text-xs leading-5 text-muted">{pillar.description}</p><div className="mt-4 flex items-center justify-between border-t border-border pt-3 text-[11px] text-muted"><span>{pillar.postCount} posts · {pillar.objective}</span><span>{pillar.industryFocus.slice(0, 2).join(" · ")}</span></div></motion.article>)}</div></section>; }