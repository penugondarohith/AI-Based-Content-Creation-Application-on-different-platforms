import type { ContentMixItem } from "@/types/content-strategy";
import { SectionHeading } from "./strategy-overview";
export function ContentMix({ items }: { items: ContentMixItem[] }) { return <section><SectionHeading eyebrow="Format distribution" title="Content mix" /><div className="mt-4 overflow-hidden rounded-xl border border-border bg-surface"><div className="grid grid-cols-[1.2fr_.4fr_.4fr_1.5fr] gap-3 border-b border-border bg-surface-muted px-4 py-3 text-[10px] font-semibold uppercase tracking-wider text-muted"><span>Format</span><span>Share</span><span>Posts</span><span>Purpose</span></div>{items.map((item) => <div key={item.contentType} className="grid grid-cols-[1.2fr_.4fr_.4fr_1.5fr] gap-3 border-b border-border px-4 py-4 text-xs last:border-0"><span className="font-medium">{item.contentType}</span><span className="text-primary">{item.percentage}%</span><span>{item.postCount}</span><span className="text-muted">{item.purpose}</span></div>)}</div></section>; }