import { Target, Users, Volume2 } from "lucide-react";
import type { ContentStrategy } from "@/types/content-strategy";
export function StrategyOverview({ strategy }: { strategy: ContentStrategy }) { return <section><SectionHeading eyebrow="Strategy intelligence" title="A clear point of view before the calendar." /><div className="mt-4 grid gap-3 md:grid-cols-3"><InfoCard icon={Target} label="Primary objective" value={strategy.primaryObjective} /><InfoCard icon={Users} label="Target audience" value={strategy.targetAudienceProfile.primaryAudience} /><InfoCard icon={Volume2} label="Communication style" value={strategy.communicationStyle.primaryTone.join(" · ")} /></div><div className="mt-3 rounded-xl border border-border bg-surface p-5"><p className="text-[10px] font-semibold uppercase tracking-wider text-muted">Brand positioning</p><p className="mt-2 text-sm leading-6 text-foreground/75">{strategy.brandPositioning}</p><div className="mt-4 flex flex-wrap gap-2">{strategy.secondaryObjectives.map((objective) => <span key={objective} className="rounded-md bg-primary-soft px-2.5 py-1.5 text-xs text-primary-strong">{objective}</span>)}</div></div></section>; }
function InfoCard({ icon: Icon, label, value }: { icon: typeof Target; label: string; value: string }) { return <div className="rounded-xl border border-border bg-surface p-5"><Icon size={18} className="text-primary" /><p className="mt-5 text-[10px] font-semibold uppercase tracking-wider text-muted">{label}</p><p className="mt-2 text-sm font-medium leading-5">{value}</p></div>; }
export function SectionHeading({ eyebrow, title }: { eyebrow: string; title: string }) { return <div><p className="text-[10px] font-semibold uppercase tracking-[.18em] text-primary">{eyebrow}</p><h2 className="mt-2 text-xl font-semibold tracking-[-.03em]">{title}</h2></div>; }