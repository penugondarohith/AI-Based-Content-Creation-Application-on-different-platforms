import type { PublishingPlan } from "@/types/content-strategy";
import { CalendarDays } from "lucide-react";
export function PublishingPlan({ plan }: { plan: PublishingPlan }) { return <div className="rounded-xl border border-border bg-surface p-5"><div className="flex items-center gap-2"><CalendarDays size={17} className="text-primary" /><h3 className="text-sm font-semibold">Publishing plan</h3></div><div className="mt-5 grid grid-cols-3 gap-3"><div><p className="text-[10px] uppercase tracking-wider text-muted">Duration</p><p className="mt-1 text-sm font-medium">{plan.duration}</p></div><div><p className="text-[10px] uppercase tracking-wider text-muted">Frequency</p><p className="mt-1 text-sm font-medium">{plan.postsPerWeek} / week</p></div><div><p className="text-[10px] uppercase tracking-wider text-muted">Cadence</p><p className="mt-1 text-sm font-medium text-success">{plan.frequencyLabel}</p></div></div><div className="mt-5 flex flex-wrap gap-2">{plan.recommendedDays.map((day) => <span key={day} className="rounded-md bg-surface-muted px-2.5 py-1.5 text-xs text-muted">{day}</span>)}</div><ul className="mt-5 space-y-2 text-xs leading-5 text-muted">{plan.strategyNotes.map((note) => <li key={note}>• {note}</li>)}</ul></div>; }