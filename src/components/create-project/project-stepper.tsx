const steps = [["01", "Industry"], ["02", "Configuration"], ["03", "Brand Context"], ["04", "Strategy"], ["05", "Content"], ["06", "Review"]];
export function ProjectStepper({ activeStep = 1 }: { activeStep?: number }) { return <div className="grid grid-cols-3 gap-1.5 rounded-xl border border-border bg-surface p-2 sm:grid-cols-6">{steps.map(([number, label], index) => <div key={number} className={`relative rounded-lg px-2 py-3 sm:px-3 ${index + 1 === activeStep ? "bg-primary-soft" : ""}`}><div className={`text-[9px] font-semibold tracking-wider sm:text-[10px] ${index + 1 === activeStep ? "text-primary" : "text-muted"}`}>STEP {number}</div><div className={`mt-1 truncate text-xs font-medium sm:text-sm ${index + 1 === activeStep ? "text-primary-strong" : "text-muted"}`}>{label}</div>{index + 1 > activeStep && <span className="absolute right-2 top-3 hidden rounded bg-surface-muted px-1.5 py-0.5 text-[9px] text-muted md:block">Soon</span>}</div>)}</div>; }