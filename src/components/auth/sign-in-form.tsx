"use client";
import { useState } from "react";
import { useRouter, useSearchParams } from "next/navigation";
import { ArrowRight, LoaderCircle, Mail } from "lucide-react";
import { useAuth } from "@/context/auth-context";
import { validateEmail } from "@/lib/auth-validation";
import { PasswordInput } from "./password-input";
import { AuthFooter } from "./auth-footer";
export function SignInForm() { const router = useRouter(); const params = useSearchParams(); const { signIn } = useAuth(); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [remember, setRemember] = useState(true); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); const notice = params.get("toast") === "signed-out" ? "You have been signed out." : ""; const submit = (event: React.FormEvent) => { event.preventDefault(); if (!validateEmail(email) || !password) { setError("Invalid email or password."); return; } setLoading(true); try { signIn(email, password); const destination = params.get("redirect")?.startsWith("/") ? params.get("redirect")! : "/dashboard"; router.push(`${destination}?toast=welcome-back`); } catch { setError("Invalid email or password."); setLoading(false); } }; return <><div className="mb-8"><p className="text-xs font-semibold uppercase tracking-[.18em] text-primary">Welcome to your workspace</p><h1 className="mt-3 text-3xl font-semibold tracking-[-.04em]">Welcome back</h1><p className="mt-2 text-sm leading-6 text-muted">Continue building your content strategy.</p></div>{notice && <p role="status" className="mb-5 rounded-lg border border-emerald-200 bg-emerald-50 px-3 py-2.5 text-xs text-success">{notice}</p>}<form onSubmit={submit} noValidate className="space-y-5"><div><label htmlFor="sign-in-email" className="text-xs font-medium">Email address</label><div className="relative mt-2"><Mail size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-muted" /><input id="sign-in-email" type="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder="you@company.com" className="w-full rounded-lg border border-border bg-surface py-3 pl-10 pr-3 text-sm outline-none transition placeholder:text-muted/60 focus:border-primary focus:ring-2 focus:ring-primary/15" /></div></div><PasswordInput id="sign-in-password" label="Password" value={password} onChange={setPassword} error={error} /><div className="flex items-center justify-between text-xs"><label className="flex items-center gap-2 text-muted"><input type="checkbox" checked={remember} onChange={(event) => setRemember(event.target.checked)} className="size-3.5 accent-primary" /> Remember me</label><button type="button" onClick={() => setError("Password recovery will be available soon.")} className="font-medium text-primary">Forgot password?</button></div>{error && !password && <p role="alert" className="text-xs text-danger">{error}</p>}<button type="submit" disabled={loading} className="flex w-full items-center justify-center gap-2 rounded-lg bg-primary px-4 py-3 text-sm font-medium text-white transition hover:bg-primary-strong disabled:cursor-not-allowed disabled:opacity-60">{loading ? <><LoaderCircle size={16} className="animate-spin" /> Signing you in...</> : <>Sign in <ArrowRight size={16} /></>}</button></form><AuthFooter mode="sign-in" /></>; }