import { dashboardStats } from "@/config/navigation";
import { projects } from "@/data/projects";
import { ProjectCard } from "@/components/dashboard/project-card";
import { QuickActions } from "@/components/dashboard/quick-actions";
import { WelcomeCard } from "@/components/dashboard/welcome-card";

export default function DashboardPage() { return <div className="mx-auto max-w-7xl px-5 py-8 lg:px-8"><WelcomeCard /><div className="mt-8 grid gap-3 md:grid-cols-3">{dashboardStats.map(({ label, value, change, icon: Icon }) => <div key={label} className="rounded-xl border border-border bg-surface p-5"><div className="flex items-center justify-between"><span className="text-sm text-muted">{label}</span><Icon size={17} className="text-primary" /></div><div className="mt-5 text-3xl font-semibold tracking-[-.04em]">{value}</div><p className="mt-1 text-xs text-success">{change}</p></div>)}</div><div className="mt-10"><div className="mb-4"><p className="text-xs font-semibold uppercase tracking-[.18em] text-primary">Shortcuts</p><h2 className="mt-2 text-lg font-semibold">Move your work forward</h2></div><QuickActions /></div><div className="mt-10"><div className="mb-4 flex items-end justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-primary">Your workspace</p><h2 className="mt-2 text-lg font-semibold">Recent projects</h2></div><a href="/projects" className="text-xs font-medium text-primary">View all</a></div><div className="grid gap-4 lg:grid-cols-3">{projects.map((project) => <ProjectCard key={project.id} project={project} />)}</div></div></div>; }