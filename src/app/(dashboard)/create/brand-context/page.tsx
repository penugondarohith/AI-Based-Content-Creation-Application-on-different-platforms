import Link from "next/link";
import { ArrowLeft, ArrowRight, FileText, LockKeyhole, Sparkles } from "lucide-react";
import { CreateProjectHeader } from "@/components/create-project/create-project-header";
import { ProjectStepper } from "@/components/create-project/project-stepper";
export default function BrandContextPage() { return <div className="mx-auto max-w-5xl px-5 py-8 lg:px-8"><CreateProjectHeader /><ProjectStepper activeStep={3} /><div className="mx-auto mt-16 max-w-2xl text-center"><div className="mx-auto flex size-16 items-center justify-center rounded-2xl bg-primary-soft text-primary"><Sparkles size={26} /></div><p className="mt-7 text-xs font-semibold uppercase tracking-[.18em] text-primary">Phase 3</p><h2 className="mt-3 text-4xl font-semibold tracking-[-.04em]">Brand Context</h2><p className="mx-auto mt-4 max-w-lg text-sm leading-6 text-muted">Your strategy is configured. The next phase will add the brand knowledge that makes every recommendation feel unmistakably yours.</p><div className="mt-10 grid gap-3 text-left sm:grid-cols-2"><div className="rounded-xl border border-border bg-surface p-5"><FileText className="text-primary" size={20} /><h3 className="mt-5 text-sm font-semibold">Brand Knowledge Vault</h3><p className="mt-2 text-xs leading-5 text-muted">Voice, audience, values, and messaging in one source of truth.</p></div><div className="rounded-xl border border-border bg-surface p-5"><LockKeyhole className="text-primary" size={20} /><h3 className="mt-5 text-sm font-semibold">Reference intelligence</h3><p className="mt-2 text-xs leading-5 text-muted">Reference files will help future strategies stay grounded in your world.</p></div></div><div className="mt-8 flex flex-wrap items-center justify-center gap-4"><Link href="/create/configure" className="inline-flex items-center gap-2 text-sm font-medium text-muted"><ArrowLeft size={15} /> Back to configuration</Link><Link href="/create/strategy" className="inline-flex items-center gap-2 rounded-lg bg-primary px-4 py-2.5 text-sm font-medium text-white transition hover:bg-primary-strong">Build content strategy <ArrowRight size={15} /></Link></div></div></div>; }