import type { ImageGenerationInput } from "@/types/image-generation-input";
import type { GeneratedImage } from "@/types/generated-image";
export interface ImageGenerationProvider { generateImage(input: ImageGenerationInput, variation?: number): Promise<GeneratedImage>; }

const dimensions = { "1:1": [1024, 1024], "4:5": [1024, 1280], "16:9": [1280, 720], "9:16": [720, 1280] } as const;
export class MockImageGenerationProvider implements ImageGenerationProvider { async generateImage(input: ImageGenerationInput, variation = 0): Promise<GeneratedImage> { const [width, height] = dimensions[input.prompt.aspectRatio]; const palette = input.prompt.visualStyle === "Cinematic" ? ["#161735", "#8b7cf6"] : input.prompt.visualStyle === "Minimal" ? ["#f7f8fc", "#4f46c8"] : ["#222149", "#c58b45"]; const escaped = (input.prompt.subject || "Strategic visual").replace(/[<>&\"']/g, ""); const shift = variation % 3; const svg = `<svg xmlns="http://www.w3.org/2000/svg" width="${width}" height="${height}" viewBox="0 0 ${width} ${height}"><defs><linearGradient id="g" x1="0" x2="1" y1="0" y2="1"><stop stop-color="${palette[0]}"/><stop offset="1" stop-color="${palette[1]}"/></linearGradient><filter id="blur"><feGaussianBlur stdDeviation="42"/></filter></defs><rect width="${width}" height="${height}" fill="url(#g)"/><circle cx="${width * (.62 + shift * .08)}" cy="${height * .26}" r="${Math.min(width, height) * .23}" fill="#ffffff" opacity=".12" filter="url(#blur)"/><circle cx="${width * .25}" cy="${height * .72}" r="${Math.min(width, height) * .18}" fill="#ffffff" opacity=".08"/><rect x="${width * .12}" y="${height * .14}" width="${width * .76}" height="${height * .72}" rx="${Math.min(width, height) * .04}" fill="#ffffff" opacity=".08" stroke="#ffffff" stroke-opacity=".3"/><path d="M${width * .2} ${height * .66} Q${width * .5} ${height * (.36 + shift * .04)} ${width * .8} ${height * .66}" fill="none" stroke="#ffffff" stroke-opacity=".55" stroke-width="${Math.max(2, width / 180)}"/><text x="${width * .12}" y="${height * .9}" fill="#ffffff" fill-opacity=".7" font-family="sans-serif" font-size="${Math.max(14, width / 42)}" letter-spacing="2">${escaped.slice(0, 30).toUpperCase()}</text></svg>`; const now = new Date().toISOString(); return { id: `image-${input.generatedContentId}-${Date.now()}`, projectId: input.projectId, generatedContentId: input.generatedContentId, calendarPostId: input.calendarPostId, status: "READY", imageUrl: `data:image/svg+xml;charset=utf-8,${encodeURIComponent(svg)}`, prompt: input.prompt, metadata: { provider: "MockImageProvider", model: "contentforge-visual-dev", width, height, generatedAt: now }, createdAt: now, updatedAt: now }; } }
export const imageGenerationProvider = new MockImageGenerationProvider();