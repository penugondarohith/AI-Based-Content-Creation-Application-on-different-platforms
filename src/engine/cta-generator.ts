import type { ContentGenerationInput } from "@/types/content-generation-input";
export function generateCta(input: ContentGenerationInput) { const goal = input.project.configuration.primaryGoal; const name = input.brandContext?.brandName ?? input.industryDNA.name; if (goal === "LEAD_GENERATION") return `Connect with ${name} to learn more.`; if (goal === "SALES_CONVERSION" || goal === "PRODUCT_PROMOTION") return `Explore ${name} today.`; if (goal === "AUDIENCE_ENGAGEMENT" || goal === "COMMUNITY_BUILDING") return "Tell us which part speaks to you."; return `Discover the story behind ${name}.`; }