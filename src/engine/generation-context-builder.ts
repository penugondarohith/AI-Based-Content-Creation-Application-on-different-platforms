import type { ContentGenerationInput } from "@/types/content-generation-input";
import type { GenerationContext } from "@/types/generated-content";
export function buildGenerationContext(input: ContentGenerationInput): GenerationContext { const pillar = input.contentStrategy.contentPillars.find((item) => item.id === input.calendarPost.contentPillarId); return { industry: input.industryDNA, brandName: input.brandContext?.brandName, contentPillar: pillar?.title ?? "Strategic content", contentTheme: input.calendarPost.contentTheme, objective: input.calendarPost.objective, recommendedFormat: input.calendarPost.recommendedFormat, targetAudience: input.brandContext?.targetAudience || input.project.configuration.customAudience || input.project.configuration.targetAudience || undefined, communicationTone: input.contentStrategy.communicationStyle.primaryTone, brandKeywords: input.brandContext?.keywords ?? input.contentStrategy.communicationStyle.vocabularyGuidelines, uniqueSellingPoints: input.brandContext?.uniqueSellingPoints ?? [] }; }