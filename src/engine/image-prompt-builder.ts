import type { ImageAspectRatio, ImageGenerationPrompt } from "@/types/generated-image";
import type { VisualGenerationContext } from "@/types/visual-generation-context";
export function buildImagePrompt(context: VisualGenerationContext, aspectRatio: ImageAspectRatio, visualStyle: string): ImageGenerationPrompt { const industryStyle: Record<string, string> = { Jewellery: "macro product photography, elegant precious materials, soft warm studio lighting", "Real Estate": "wide-angle architectural photography, natural daylight, premium spaces and location context", Perfume: "cinematic fragrance editorial, atmospheric shadows, sensory lifestyle storytelling", "FMCG / Food": "high-quality commercial food photography, fresh ingredients, texture and appetite appeal" }; const subject = context.products?.[0] ?? context.visualDirection.subjectFocus; const prompt = `${subject}, ${context.visualDirection.concept}, ${context.visualDirection.composition}, mood of ${context.visualDirection.mood.join(", ")}, ${industryStyle[context.industry.name] ?? "premium editorial campaign photography"}, background: ${context.visualDirection.backgroundSuggestion}, visual style: ${visualStyle}, aligned to ${context.contentTheme} and ${context.objective}, professional composition, high quality commercial visual, no text in image`; return { prompt, negativePrompt: "text, logo, watermark, caption, headline, distorted objects, low quality, cluttered composition", aspectRatio, visualStyle, mood: context.visualDirection.mood, subject, composition: context.visualDirection.composition }; }